import { ActionTypes } from '../_actions/notes.actions';
import { ActionModel } from './../_models/action.model';
import { Note } from './../_models/note.model';


export const initialState: Note[] = []

export function notesReducer(state = initialState, action: ActionModel){
    // switch (action.type) {
    //     case ActionTypes.GetAll:
            
    // }
}

async function getAll(){

}